const MAX_STRING_SIZE = 255;
const MIN_PASSWORD_SIZE = 8;
const MAX_PASSWORD_SIZE = 64;

export const textConstants = {
  maxStringSize: MAX_STRING_SIZE,
  minPasswordSize: MIN_PASSWORD_SIZE,
  maxPasswordSize: MAX_PASSWORD_SIZE,
};
