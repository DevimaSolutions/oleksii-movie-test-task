/**
 * hooks folder contains custom hooks that extend default Selector and Dispatch hooks with types
 */

export { default as useSelector } from './useSelector';
export { default as useDispatch } from './useDispatch';
